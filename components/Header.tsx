import Logo from "./Logo";
import MenuBtn from "./MenuBtn";

const Header = () => {
  return (
    <div className="header">
      <Logo />
      <MenuBtn />
    </div>
  );
};

export default Header;
